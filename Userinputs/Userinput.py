# The input function prints something in the terminal, then waits for the user to enter a value and what ever the user enters the input function returns 
name = input('What is your name? ')
# This is another example of an expression(a piece of code that produces a value )
print('Hi ' + name )